os: mac
and app.bundle: com.apple.Terminal
-
clean repeat: key(cmd-k up enter)
wheel upper:
    user.mouse_scroll_up_continuous()
    user.mouse_scroll_up_continuous()
wheel downer:
    user.mouse_scroll_down_continuous()
    user.mouse_scroll_down_continuous()
